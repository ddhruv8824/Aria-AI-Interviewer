# Local development config (copy from judge0.conf.example for production passwords)

POSTGRES_HOST=db
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=judge0_local_dev

REDIS_HOST=redis
REDIS_PASSWORD=judge0_redis_dev

MAX_QUEUE_SIZE=100
